#!/bin/bash
set -e

#1. check for needed things like {xcode cm tools, homebrew, etc}
#2. install the things that we can from cli
#3. downloadd things that I can from cli
#4. run git submodule init and git submodule update
#5. setup remainder of git's global config file

#6. mkdir working directory structures
#7. generate a list of projects that I always clone (perhaps not into projects directly)
#8. 


#possible brew installs
	#:python
	#:git
	#:vim
	#:chruby
	#:ruby-install

#possible git repos for new mac
	#:slate
	#:powerline
	#:powerline-fonts




#cd /Applications && curl http://www.ninjamonkeysoftware.com/slate/versions/slate-latest.tar.gz | tar -xz

